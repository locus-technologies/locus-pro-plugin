name: Validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    name: Validate plugin
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate Claude plugin and marketplace manifests
        run: |
          npx -y @anthropic-ai/claude-code plugin validate . --strict
          npx -y @anthropic-ai/claude-code plugin validate .claude-plugin/marketplace.json --strict

      - name: Check manifest consistency
        run: node scripts/check-manifests.mjs

  validate-grok:
    name: Validate Grok plugin manifest
    runs-on: ubuntu-latest
    env:
      GROK_BIN_DIR: ${{ github.workspace }}/.grok-bin
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Grok CLI
        env:
          GROK_INSTALLER_SHA256: e11ef1692ece18489a450ee918bdb7dd0b229d001c08d9c2b1387fffd78a0aa9
        run: |
          curl -fsSL https://x.ai/cli/install.sh -o grok-install.sh
          echo "$GROK_INSTALLER_SHA256  grok-install.sh" | shasum -a 256 -c -
          bash grok-install.sh 1.0.13
          echo "$GROK_BIN_DIR" >> "$GITHUB_PATH"

      - name: Validate Grok plugin manifest
        run: |
          grok version
          grok plugin validate .
